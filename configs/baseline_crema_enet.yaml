# Frame-average baseline: CREMA-D val split, EmotiEffLib enet head.
# Consumed by src.eval_frame_average_baseline.evaluate(cfg).
data:
  val_annotations: data/crema_d/annotations/val.txt
features_cache: cache/features/crema_d/enet_b0_8_va_mtl
